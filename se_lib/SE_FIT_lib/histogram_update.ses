// must keep the lines
// printf "histogram(edge where not fixed, length)\n"
// printf "histogram(facet where not fixed, area)\n"
// otherwise it will not work.

histogram_update := {
			printf "histogram_form_update_request\n";
			printf "histogram(edge where not fixed, length)\n";
			histogram(edge where not fixed, length);
			printf "histogram(facet where not fixed, area)\n";
			histogram(facet where not fixed, area);
			}